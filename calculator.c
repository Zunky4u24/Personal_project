#include <stdio.h>
#include <math.h>
/* this is a simple calculator
 * its contains the code
 */

int main(void)
{
	int x
	int y

	int z = y + x
	int a = y - x
	int b = y * x
	int c = y / x

	scanf("%d", x, y);
	printf("%d", z)
}
